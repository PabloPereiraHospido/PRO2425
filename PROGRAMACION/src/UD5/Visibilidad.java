package UD5;

public class Visibilidad {
    public static void main(String[] args) {
        Persona manuel = new Alumno();
        // manuel.edad=30;
        // manuel.nombre="Manuel";
        // manuel.apellido="Perez";
        //manuel.dni no es accesible al ser protected
        //manuel.fechaNacimiento no es accesible al ser protected
        //manuel.cuentaBancaria no es accesible al ser protected
        //manuel.direccionPostal no es accesible al ser default
        //manuel.email no es accesible al ser default
        //manuel.telefono no es accesible al ser private
        //manuel.grupoSanguineo no es accesible al ser private
        //manuel.sexo no es accesible al ser private
    }
}
